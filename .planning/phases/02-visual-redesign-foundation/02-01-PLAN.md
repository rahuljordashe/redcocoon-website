---
phase: 02-visual-redesign-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/layouts/BaseLayout.astro
autonomous: true
requirements: [VIS-02, VIS-04, VIS-06, VIS-07]

must_haves:
  truths:
    - "Fonts load from local bundles, not Google Fonts CDN — no network request to fonts.googleapis.com"
    - "No FOIT (flash of invisible text) — text is visible immediately on first paint via font-display:swap"
    - "Body text renders at 1rem (16px), not 0.92rem"
    - "Variable font family names are correctly mapped in CSS custom properties"
    - "All CSS token values in :root use semantic naming — no orphaned hardcoded hex in global.css"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Updated design tokens — font family names for variable fonts, --text-base: 1rem, --space-6xl, shadow tokens"
      contains: "Inter Variable"
    - path: "src/layouts/BaseLayout.astro"
      provides: "Self-hosted font imports via @fontsource, Google Fonts links removed"
      contains: "@fontsource"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "@fontsource packages"
      via: "import statements in frontmatter"
      pattern: "import '@fontsource"
    - from: "src/styles/global.css"
      to: "src/layouts/BaseLayout.astro"
      via: "CSS custom properties referencing font families injected by @fontsource"
      pattern: "--font-elegant.*Cormorant Garamond Variable"
---

<objective>
Migrate fonts from Google Fonts CDN to self-hosted @fontsource packages and audit/update all CSS design tokens in global.css for the redesign.

Purpose: Establishes the typography and token foundation that every subsequent plan and component depends on. Fonts must load locally (VIS-06), with no FOIT (VIS-07), at the correct sizes (VIS-04), using properly audited tokens (VIS-02).

Output: Updated global.css with correct variable font names, bumped text-base, new spacing/shadow tokens. Updated BaseLayout.astro with @fontsource imports replacing Google Fonts CDN links.
</objective>

<execution_context>
@C:/Users/pcadmin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pcadmin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-visual-redesign-foundation/02-CONTEXT.md
@.planning/phases/02-visual-redesign-foundation/02-RESEARCH.md

@src/styles/global.css
@src/layouts/BaseLayout.astro

<interfaces>
<!-- Current CSS tokens in global.css :root that will be updated -->
From src/styles/global.css:
```css
--font-serif:    'DM Serif Display', 'Georgia', serif;
--font-elegant:  'Cormorant Garamond', 'Georgia', serif;
--font-sans:     'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
--text-base: 0.92rem;
/* Spacing scale ends at --space-5xl: 8rem */
```

From src/layouts/BaseLayout.astro (lines 54-57, the Google Fonts block to remove):
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=Inter:wght@300;400;500;600&family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;1,400;1,500&display=swap" rel="stylesheet">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @fontsource packages and migrate BaseLayout font loading</name>
  <files>src/layouts/BaseLayout.astro, package.json</files>
  <action>
1. Install the three @fontsource packages:
   ```
   npm install @fontsource/dm-serif-display @fontsource-variable/cormorant-garamond @fontsource-variable/inter
   ```

2. In `src/layouts/BaseLayout.astro` frontmatter (between the `---` fences), add these imports at the top, before other imports:
   ```
   import '@fontsource/dm-serif-display/400.css';
   import '@fontsource/dm-serif-display/400-italic.css';
   import '@fontsource-variable/cormorant-garamond';
   import '@fontsource-variable/inter';
   ```

3. In the `<head>` section of BaseLayout.astro, DELETE the three Google Fonts lines (the two preconnect links and the fonts.googleapis.com stylesheet link):
   - `<link rel="preconnect" href="https://fonts.googleapis.com">`
   - `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>`
   - `<link href="https://fonts.googleapis.com/css2?family=...` (the long stylesheet URL)

Do NOT remove any other `<link>` tags (favicon, preload, canonical are unrelated).

IMPORTANT: The @fontsource-variable packages use different CSS family names than the non-variable versions:
- `@fontsource-variable/cormorant-garamond` registers as `'Cormorant Garamond Variable'`
- `@fontsource-variable/inter` registers as `'Inter Variable'`
- `@fontsource/dm-serif-display` keeps the original name `'DM Serif Display'`

These name changes will be handled in Task 2 (global.css token update).
  </action>
  <verify>
    <automated>cd "C:/Users/pcadmin/Developer/Redcocoon Website/redcocoon-website" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>@fontsource packages installed, BaseLayout imports fonts from local packages, no Google Fonts CDN links remain in BaseLayout.astro head</done>
</task>

<task type="auto">
  <name>Task 2: Audit and update global.css design tokens</name>
  <files>src/styles/global.css</files>
  <action>
Update the `:root` CSS custom properties in `src/styles/global.css` with these specific changes:

**Typography — Font Family Names (match @fontsource-variable registered names):**
```css
--font-serif:    'DM Serif Display', 'Georgia', serif;          /* unchanged */
--font-elegant:  'Cormorant Garamond Variable', 'Georgia', serif; /* CHANGED from 'Cormorant Garamond' */
--font-sans:     'Inter Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; /* CHANGED from 'Inter' */
```

**Typography — Base Font Size:**
```css
--text-base: 1rem;  /* CHANGED from 0.92rem — per locked decision */
```

**Spacing — Add new token for generous section padding:**
```css
--space-6xl: 10rem;  /* NEW — for extra generous desktop section padding */
```

**Shadows — Add new semantic shadow tokens (used by Plan 02 for cards and CTAs):**
```css
/* Shadows */
--shadow-card:       0 4px 20px rgba(0, 0, 0, 0.06);
--shadow-card-hover: 0 12px 35px rgba(0, 0, 0, 0.1);
--shadow-cta:        0 8px 25px rgba(0, 0, 0, 0.12);
```

**Border Radius — Add image-specific token:**
```css
--border-radius-img: 12px;  /* NEW — per locked decision for image treatment */
```

**Body base font size** — update the `body` rule to use the token:
```css
body {
  font-size: var(--text-base); /* ADD this line — ensures body text uses the token */
}
```

DO NOT change:
- Color tokens (those are correct and used as-is)
- Transition tokens
- Noise texture
- Any keyframe animations
- Button styles (touch targets are Plan 02)
- Responsive breakpoint rules

Verify no hardcoded hex values exist in global.css outside the `:root` block (the `.btn-whatsapp` `#fff` and `.btn-whatsapp:hover` `#1fb855` are acceptable — they are brand colors for WhatsApp and Instagram button text).
  </action>
  <verify>
    <automated>cd "C:/Users/pcadmin/Developer/Redcocoon Website/redcocoon-website" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>global.css has updated font family names matching @fontsource-variable packages, --text-base is 1rem, --space-6xl exists, shadow tokens exist, --border-radius-img exists, body uses font-size: var(--text-base), build passes cleanly</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. `grep -c "fonts.googleapis.com" src/layouts/BaseLayout.astro` returns 0
3. `grep "fontsource" src/layouts/BaseLayout.astro` shows 4 import lines
4. `grep "Inter Variable" src/styles/global.css` returns a match
5. `grep "Cormorant Garamond Variable" src/styles/global.css` returns a match
6. `grep "text-base: 1rem" src/styles/global.css` returns a match
7. `grep "space-6xl" src/styles/global.css` returns a match
8. `grep "shadow-card" src/styles/global.css` returns a match
</verification>

<success_criteria>
- Fonts are self-hosted via @fontsource (no Google Fonts CDN dependency)
- Variable font family names are correctly mapped in CSS tokens
- Body text is 1rem, not 0.92rem
- New spacing and shadow tokens are available for Plan 02
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-redesign-foundation/02-01-SUMMARY.md`
</output>

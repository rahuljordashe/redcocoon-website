---
phase: 04-workshop-hospitality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Hospitality.astro
autonomous: true
requirements: [HOSP-01, HOSP-02]

must_haves:
  truths:
    - "A hospitality client reads messaging clearly addressed to their context -- custom ware, volume, collaboration"
    - "A hospitality client can initiate a B2B inquiry via email with a pre-filled subject line"
    - "A hospitality client can still use the existing WhatsApp 'Discuss a Project' button"
  artifacts:
    - path: "src/components/Hospitality.astro"
      provides: "Expanded B2B section with service bullets and email CTA"
      contains: "mailto:hello@redcocoon.com"
  key_links:
    - from: "src/components/Hospitality.astro"
      to: "mailto:hello@redcocoon.com?subject=Hospitality%20Inquiry"
      via: "email CTA button href"
      pattern: "mailto:hello@redcocoon.com"
    - from: "src/components/Hospitality.astro"
      to: "whatsapp.ts getQuoteLink()"
      via: "existing WhatsApp CTA (unchanged)"
      pattern: "getQuoteLink"
---

<objective>
Expand the Hospitality homepage section with concrete B2B service offerings and a secondary email inquiry channel alongside the existing WhatsApp CTA.

Purpose: Give hotel/restaurant buyers clear messaging about what Redcocoon offers for venues (custom dinnerware, serving pieces, full collections) and a professional email option for B2B inquiries.
Output: Updated Hospitality.astro with service bullet list and email CTA button.
</objective>

<execution_context>
@C:/Users/pcadmin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pcadmin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-workshop-hospitality/04-CONTEXT.md

<interfaces>
<!-- Current Hospitality.astro structure the executor needs to understand -->

From src/components/Hospitality.astro:
```astro
---
import { Image } from 'astro:assets';
import { getQuoteLink } from '../utils/whatsapp';
import dinnerSetsImg from '../assets/images/Dinner-sets5.jpg';
import teaCoffeeImg from '../assets/images/Tea-coffee-set5.jpg';

const quoteLink = getQuoteLink();
---
```

Current HTML structure:
- `.hospitality-tag`: "For Hotels & Restaurants"
- `.hospitality-title`: "Tableware as unique as your venue"
- `.hospitality-desc`: single paragraph about working with chefs and hoteliers
- `.hospitality-actions`: contains only `btn btn-primary` "Discuss a Project" WhatsApp link
- `.hospitality-ghost` CSS class: already defined (lines 90-98), ready for use on email CTA

From src/utils/whatsapp.ts:
```typescript
export function getQuoteLink(): string // returns WhatsApp link with B2B pre-filled message
```

Footer already uses hello@redcocoon.com for email link -- consistent email address.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add service bullets and email CTA to Hospitality section</name>
  <files>src/components/Hospitality.astro</files>
  <action>
Modify `src/components/Hospitality.astro` to add two things below the existing `.hospitality-desc` paragraph:

**1. Service bullet list (after `.hospitality-desc`, before `.hospitality-actions`):**

Add an unordered list with class `hospitality-services`:
```html
<ul class="hospitality-services">
  <li>Custom dinnerware sets for your menu</li>
  <li>Serving platters &amp; accent pieces</li>
  <li>Full venue ceramic collections</li>
</ul>
```
Claude's discretion on exact wording -- these are illustrative. Keep it concise (2-3 bullets), B2B-focused, and specific enough that a hotel buyer can picture the product.

**2. Email CTA button (inside `.hospitality-actions`, after the existing WhatsApp button):**

Add an email button using the existing `.hospitality-ghost` CSS class (already defined in this component at lines 90-98):
```astro
<a
  href="mailto:hello@redcocoon.com?subject=Hospitality%20Inquiry"
  class="btn btn-ghost hospitality-ghost"
>
  Email Us
</a>
```

Do NOT modify the existing "Discuss a Project" WhatsApp button -- it stays as-is.

**3. Add scoped CSS for `.hospitality-services`:**

Add these styles to the existing `<style>` block:
```css
.hospitality-services {
  list-style: none;
  padding: 0;
  margin-bottom: var(--space-2xl);
  display: flex;
  flex-direction: column;
  gap: var(--space-sm);
}

.hospitality-services li {
  color: var(--color-beige);
  font-size: var(--text-sm);
  padding-left: 1.5rem;
  position: relative;
  line-height: 1.6;
}

.hospitality-services li::before {
  content: '';
  position: absolute;
  left: 0;
  top: 0.55em;
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--color-terracotta-light);
}
```

The bullet color uses terracotta-light to match the tag text, maintaining the dark-section color hierarchy (espresso bg > cream title > beige desc > terracotta-light accents).

Note: The `.hospitality-desc` currently has `margin-bottom: var(--space-2xl)`. Reduce it to `margin-bottom: var(--space-lg)` so the bullets sit closer to the description paragraph (the bullet list gets its own margin-bottom: var(--space-2xl) before the actions).
  </action>
  <verify>
    <automated>cd "C:/Users/pcadmin/Developer/Redcocoon Website/redcocoon-website" && bun run build 2>&1 | tail -5</automated>
  </verify>
  <done>
- Hospitality section shows 2-3 concrete service bullets below the description
- Bullets have terracotta-light dot markers on dark espresso background
- "Email Us" button appears next to "Discuss a Project" in .hospitality-actions
- Email button links to mailto:hello@redcocoon.com?subject=Hospitality%20Inquiry
- Email button uses .hospitality-ghost styling (white/translucent on dark bg)
- Existing WhatsApp "Discuss a Project" button is unchanged
- Build passes with zero errors
  </done>
</task>

</tasks>

<verification>
1. `bun run build` completes with zero errors
2. `npx astro check` reports no new TypeScript errors
3. Hospitality section on homepage shows service bullets and two CTA buttons
</verification>

<success_criteria>
- A hospitality client reads specific service offerings (custom dinnerware, platters, venue collections) in bullet form
- A hospitality client sees two inquiry options: WhatsApp "Discuss a Project" and "Email Us"
- The email CTA opens the user's email client with hello@redcocoon.com and "Hospitality Inquiry" as the subject
- The existing WhatsApp button behavior is preserved unchanged
- The site builds cleanly with `bun run build`
</success_criteria>

<output>
After completion, create `.planning/phases/04-workshop-hospitality/04-02-SUMMARY.md`
</output>

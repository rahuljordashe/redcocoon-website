---
phase: 03-gallery-products
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/config.ts
  - src/content/products/enamel-vase.md
  - src/content/products/wall-decor-plate.md
  - src/assets/images/Bowls5.jpg
  - src/assets/images/Chip-dip-sets4.jpg
  - src/assets/images/mugs-fatured-image.jpg
  - src/assets/images/Vases3.jpg
  - src/components/LatestPieces.astro
autonomous: true
requirements: [GAL-03, GAL-04, GAL-07, GAL-08]

must_haves:
  truths:
    - "Content schema includes madeToOrder boolean field with false default"
    - "At least 1-2 products are marked as sold (stock: 0) with madeToOrder: true"
    - "All product images exist in src/assets/images/ for Astro Image pipeline"
    - "LatestPieces uses product.id instead of deprecated product.slug"
  artifacts:
    - path: "src/content/config.ts"
      provides: "Product schema with madeToOrder field"
      contains: "madeToOrder"
    - path: "src/content/products/enamel-vase.md"
      provides: "MTO example product (stock: 0, madeToOrder: true)"
      contains: "madeToOrder: true"
    - path: "src/assets/images/Bowls5.jpg"
      provides: "Product image for bowl-terracotta and cocoon-serving-bowl"
    - path: "src/assets/images/Vases3.jpg"
      provides: "Product image for vase-sand"
  key_links:
    - from: "src/content/config.ts"
      to: "src/content/products/*.md"
      via: "Zod schema validation at build time"
      pattern: "madeToOrder.*z\\.boolean"
    - from: "src/components/LatestPieces.astro"
      to: "src/pages/shop/[slug].astro"
      via: "product.id used in href"
      pattern: "product\\.id"
---

<objective>
Add madeToOrder field to the product content schema, migrate remaining product images to src/assets/ for Astro Image optimization, mark 1-2 products as sold/MTO examples, and standardize on product.id in existing code.

Purpose: Establish the data foundation that all gallery and product detail plans depend on. Without the madeToOrder field, stock badge logic and commission CTAs cannot differentiate sold-out pieces. Without images in src/assets/, Astro Image cannot optimize product photos.
Output: Updated schema, 2 MTO example products, 4 new images in src/assets/images/, LatestPieces using product.id
</objective>

<execution_context>
@C:/Users/pcadmin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pcadmin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gallery-products/03-CONTEXT.md
@.planning/phases/03-gallery-products/03-RESEARCH.md
@.planning/phases/02-visual-redesign-foundation/02-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/content/config.ts (CURRENT — will be modified):
```typescript
const products = defineCollection({
  type: 'content',
  schema: z.object({
    name: z.string(),
    code: z.string(),
    price: z.number(),
    currency: z.string().default('LKR'),
    collection: z.string(),
    category: z.enum(['cups','plates','bowls','vases','sets','coasters','decor','kitchen']),
    material: z.string().default('Stoneware Clay'),
    stock: z.number().default(0),
    images: z.array(z.string()),
    featured: z.boolean().default(false),
    description: z.string().optional(),
  }),
});
```

From src/components/LatestPieces.astro (line 22 — uses deprecated .slug):
```typescript
slug={product.slug}   // Must change to product.id
```

From src/components/ProductCard.astro Props:
```typescript
interface Props {
  name: string;
  code: string;
  price: number;
  currency?: string;
  images: string[];
  stock: number;
  slug: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add madeToOrder to schema and update product frontmatter</name>
  <files>src/content/config.ts, src/content/products/enamel-vase.md, src/content/products/wall-decor-plate.md</files>
  <action>
1. In `src/content/config.ts`, add `madeToOrder: z.boolean().default(false)` to the product schema object, after the `featured` field. The default(false) means existing products need no frontmatter change — only MTO examples need the explicit field.

2. In `src/content/products/enamel-vase.md`, change `stock: 1` to `stock: 0` and add `madeToOrder: true` to the frontmatter. This makes Enamel Vase a sold-out Made to Order piece.

3. In `src/content/products/wall-decor-plate.md`, change `stock: 3` to `stock: 0` and add `madeToOrder: true` to the frontmatter. This makes Wall Decor Plate a second MTO example.

These two products were chosen because they are non-featured items (won't break the homepage LatestPieces display) and represent different collections (Monsoon Series, Wabi-Sabi Home) for filter testing variety.

Do NOT add `madeToOrder: false` to every product file — the schema default handles that automatically.
  </action>
  <verify>
    <automated>cd "C:/Users/pcadmin/Developer/Redcocoon Website/redcocoon-website" && bun run build 2>&1 | tail -5</automated>
  </verify>
  <done>Schema has madeToOrder field, enamel-vase and wall-decor-plate have stock: 0 + madeToOrder: true, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Copy remaining product images to src/assets and standardize LatestPieces to product.id</name>
  <files>src/assets/images/Bowls5.jpg, src/assets/images/Chip-dip-sets4.jpg, src/assets/images/mugs-fatured-image.jpg, src/assets/images/Vases3.jpg, src/components/LatestPieces.astro</files>
  <action>
1. Copy the 4 product images that are NOT yet in src/assets/images/ from public/images/:
   - `public/images/Bowls5.jpg` -> `src/assets/images/Bowls5.jpg`
   - `public/images/Chip-dip-sets4.jpg` -> `src/assets/images/Chip-dip-sets4.jpg`
   - `public/images/mugs-fatured-image.jpg` -> `src/assets/images/mugs-fatured-image.jpg` (keep the typo'd filename — matches existing product frontmatter)
   - `public/images/Vases3.jpg` -> `src/assets/images/Vases3.jpg`

   After this, ALL product images referenced in product MD frontmatter will have corresponding files in src/assets/images/. The public/images/ originals stay (do NOT delete them).

   Images already in src/assets/images/ (from Phase 2): Coasters18.jpg, Dinner-sets5.jpg, Enamel.jpg, Kitchen-ware1.jpg, Tea-coffee-set5.jpg, Toast-Plates-new.jpg, Wall-deco5.jpg

2. In `src/components/LatestPieces.astro`, change line 22 from `slug={product.slug}` to `slug={product.id}`. This standardizes on the Astro 5 canonical property. The values are identical for these filenames (e.g., "bowl-terracotta") but .id is the correct API going forward.

Do NOT modify ProductCard.astro's prop name from `slug` — it still receives a string; only the source value changes from `.slug` to `.id`.
  </action>
  <verify>
    <automated>cd "C:/Users/pcadmin/Developer/Redcocoon Website/redcocoon-website" && ls src/assets/images/Bowls5.jpg src/assets/images/Chip-dip-sets4.jpg src/assets/images/mugs-fatured-image.jpg src/assets/images/Vases3.jpg 2>&1 && bun run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 4 images exist in src/assets/images/, LatestPieces passes product.id to ProductCard, build passes cleanly</done>
</task>

</tasks>

<verification>
- `bun run build` passes without errors
- `src/content/config.ts` contains `madeToOrder: z.boolean().default(false)`
- `src/content/products/enamel-vase.md` has `stock: 0` and `madeToOrder: true`
- `src/content/products/wall-decor-plate.md` has `stock: 0` and `madeToOrder: true`
- All 16 product images now exist in `src/assets/images/` (12 from Phase 2 + 4 new)
- `src/components/LatestPieces.astro` uses `product.id` not `product.slug`
</verification>

<success_criteria>
Product content schema supports madeToOrder field, 2 products demonstrate sold/MTO state, all product images are available for Astro Image optimization, and existing code uses canonical Astro 5 property.
</success_criteria>

<output>
After completion, create `.planning/phases/03-gallery-products/03-01-SUMMARY.md`
</output>

---
phase: 01-critical-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ReservationFlow.astro
  - src/components/FloatingContactButton.astro
  - src/components/Footer.astro
  - src/components/Hospitality.astro
autonomous: true
requirements:
  - FIX-01
  - FIX-02

must_haves:
  truths:
    - "Clicking the reservation channels in ReservationFlow shows only Instagram DM and WhatsApp (no Form badge)"
    - "The FloatingContactButton popup shows only Instagram DM and WhatsApp (no Reservation Form option)"
    - "The Footer CTA banner shows only Instagram DM and WhatsApp buttons (no Reservation Form button)"
    - "Clicking the Hospitality CTA button opens WhatsApp with the B2B pre-filled message"
    - "There is exactly one CTA button in the Hospitality section, labeled Discuss a Project"
    - "The deployed site has zero broken internal navigation links"
  artifacts:
    - path: "src/components/ReservationFlow.astro"
      provides: "Reservation flow with 2-channel badges (IG + WA)"
      contains: "channel-badge channel-instagram"
    - path: "src/components/FloatingContactButton.astro"
      provides: "Mobile FAB with 2 contact options (IG + WA)"
      contains: "floating-option floating-ig"
    - path: "src/components/Footer.astro"
      provides: "Footer CTA banner with 2 buttons (IG + WA)"
      contains: "btn btn-instagram"
    - path: "src/components/Hospitality.astro"
      provides: "Hospitality section with single WhatsApp CTA"
      contains: "Discuss a Project"
  key_links:
    - from: "src/components/Hospitality.astro"
      to: "src/utils/whatsapp.ts"
      via: "getQuoteLink() import already in frontmatter"
      pattern: "quoteLink.*btn-primary.*Discuss a Project"
---

<objective>
Patch two live broken navigation links on the Redcocoon website.

Purpose: The site currently has a `#contact-form` anchor (points to nothing) in three components and a `/hospitality` link (returns 404) in the Hospitality section. These dead ends must be removed before any new milestone work begins.

Output: Four modified Astro components with all broken links removed, resulting in zero dead-end navigation paths on the live site.
</objective>

<execution_context>
@C:/Users/pcadmin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pcadmin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-critical-fixes/01-CONTEXT.md
@.planning/phases/01-critical-fixes/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove broken #contact-form links from 3 components (FIX-01)</name>
  <files>src/components/ReservationFlow.astro, src/components/FloatingContactButton.astro, src/components/Footer.astro</files>
  <action>
Remove the `#contact-form` anchor element from three components. Leave all CSS class definitions (`.channel-form`, `.floating-form`) intact in their scoped `<style>` blocks — they will be needed when Phase 4 restores the form link.

**ReservationFlow.astro (line 28):** Delete the entire line:
```
<a href="#contact-form" class="channel-badge channel-form">Form</a>
```
The `reservation-channels` div should contain only the Instagram DM and WhatsApp badges after this edit.

**FloatingContactButton.astro (line 19):** Delete the entire line:
```
<a href="#contact-form" class="floating-option floating-form">Reservation Form</a>
```
The `floating-popup` div should contain only the title, Instagram DM link, and WhatsApp link after this edit. The FloatingContactButton remains a mini-menu with 2 options per user decision (do NOT simplify to a single link).

**Footer.astro (line 13):** Delete the entire line:
```
<a href="#contact-form" class="btn btn-ghost">Reservation Form</a>
```
The `footer-cta-buttons` div should contain only the Instagram DM and WhatsApp buttons after this edit.

**Do NOT:**
- Remove any CSS class definitions (`.channel-form`, `.floating-form`, `.btn-ghost`) from style blocks
- Add a placeholder `<section id="contact-form">` anywhere
- Change the FloatingContactButton to a single-action button
  </action>
  <verify>
    <automated>cd "C:/Users/pcadmin/Developer/Redcocoon Website/redcocoon-website" && npx astro check && npm run build</automated>
  </verify>
  <done>All three components build cleanly. No `href="#contact-form"` exists in any source file. ReservationFlow has 2 channel badges, FloatingContactButton has 2 popup options, Footer CTA has 2 buttons.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate Hospitality section to single WhatsApp CTA (FIX-02)</name>
  <files>src/components/Hospitality.astro</files>
  <action>
Replace the two-button `hospitality-actions` block with a single primary CTA. The `quoteLink` variable is already declared in the component frontmatter (`const quoteLink = getQuoteLink()`) so no import changes are needed.

**Hospitality.astro (lines 14-17):** Replace the current two-button block:
```astro
<div class="hospitality-actions">
  <a href="/hospitality" class="btn btn-primary">View Portfolio</a>
  <a href={quoteLink} target="_blank" rel="noopener noreferrer" class="btn btn-ghost hospitality-ghost">Request a Quote</a>
</div>
```

With this single-button block:
```astro
<div class="hospitality-actions">
  <a href={quoteLink} target="_blank" rel="noopener noreferrer" class="btn btn-primary">Discuss a Project</a>
</div>
```

The button uses `btn-primary` class (existing). The flex container will display the single button left-aligned within the text-left layout, which is correct. No CSS layout changes needed.

**Do NOT:**
- Change the `getQuoteLink` import or rename it to `getHospitalityWhatsAppLink` (they are the same function; use the existing name)
- Remove the `.hospitality-ghost` CSS from the style block (harmless orphan, may be reused in Phase 4)
  </action>
  <verify>
    <automated>cd "C:/Users/pcadmin/Developer/Redcocoon Website/redcocoon-website" && npx astro check && npm run build</automated>
  </verify>
  <done>Hospitality section has exactly one CTA button labeled "Discuss a Project". The button links to WhatsApp with the B2B pre-filled message via `quoteLink`. No `/hospitality` href exists in any source file. Build passes cleanly.</done>
</task>

</tasks>

<verification>
After both tasks complete, run a full verification:

1. **Build check:** `npx astro check && npm run build` — must pass with zero errors
2. **Grep for broken links:** Confirm no `#contact-form` or `/hospitality` href remains in any `.astro` file:
   - `grep -r "#contact-form" src/` should return zero results
   - `grep -r '"/hospitality"' src/` should return zero results
3. **Grep for expected content:**
   - `grep "Discuss a Project" src/components/Hospitality.astro` should match
   - ReservationFlow.astro should contain exactly 2 `channel-badge` anchor elements
   - FloatingContactButton.astro should contain exactly 2 `floating-option` anchor elements
   - Footer.astro `footer-cta-buttons` should contain exactly 2 `btn` anchor elements
</verification>

<success_criteria>
- Zero broken internal navigation links in the built site
- `npm run build` passes cleanly
- No `href="#contact-form"` in any source file
- No `href="/hospitality"` in any source file
- Hospitality section has single "Discuss a Project" CTA linking to WhatsApp
- FloatingContactButton retains 2-option mini-menu (Instagram DM + WhatsApp)
- All orphaned CSS classes (.channel-form, .floating-form, .hospitality-ghost) are preserved for Phase 4
</success_criteria>

<output>
After completion, create `.planning/phases/01-critical-fixes/01-01-SUMMARY.md`
</output>
